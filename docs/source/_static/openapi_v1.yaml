2025-06-03 21:52:57 INFO     aignostics.aignostics.utils.boot ⭐ Booting aignostics v0.2.30 (project root /Users/helmut/Code/python-sdk, pid 31680), parent 'Python' (pid   boot.py:87
                             31653)                                                                                                                                                   
components:
  schemas:
    ApplicationReadResponse:
      properties:
        application_id:
          description: Application ID
          examples:
          - h-e-tme
          title: Application Id
          type: string
        description:
          description: Application documentations
          title: Description
          type: string
        name:
          description: Application display name
          examples:
          - HETA
          title: Name
          type: string
        regulatory_classes:
          description: Regulatory class, to which the applications compliance
          examples:
          - - RuO
          items:
            type: string
          title: Regulatory Classes
          type: array
      required:
      - application_id
      - name
      - regulatory_classes
      - description
      title: ApplicationReadResponse
      type: object
    ApplicationRunStatus:
      enum:
      - canceled_system
      - canceled_user
      - completed
      - completed_with_error
      - received
      - rejected
      - running
      - scheduled
      title: ApplicationRunStatus
      type: string
    ApplicationVersionReadResponse:
      properties:
        application_id:
          description: Application ID
          title: Application Id
          type: string
        application_version_id:
          description: Application version ID
          examples:
          - h-e-tme:v0.0.1
          title: Application Version Id
          type: string
        changelog:
          description: Description of the changes relative to the previous version
          title: Changelog
          type: string
        flow_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          description: Flow ID, used internally by the platform
          title: Flow Id
        input_artifacts:
          description: List of the input fields, provided by the User
          items:
            $ref: '#/components/schemas/InputArtifactReadResponse'
          title: Input Artifacts
          type: array
        output_artifacts:
          description: List of the output fields, generated by the application
          items:
            $ref: '#/components/schemas/OutputArtifactReadResponse'
          title: Output Artifacts
          type: array
        version:
          description: Semantic version of the application
          title: Version
          type: string
      required:
      - application_version_id
      - version
      - application_id
      - changelog
      - input_artifacts
      - output_artifacts
      title: ApplicationVersionReadResponse
      type: object
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          title: Detail
          type: array
      title: HTTPValidationError
      type: object
    InputArtifactCreationRequest:
      properties:
        download_url:
          description: '[Signed URL](https://cloud.google.com/cdn/docs/using-signed-urls)
            to the input artifact file. The URL should be valid for at least 6 days
            from the payload submission time.'
          examples:
          - https://example.com/case-no-1-slide.tiff
          format: uri
          maxLength: 2083
          minLength: 1
          title: Download Url
          type: string
        metadata:
          description: The metadata of the artifact, required by the application version.
            The JSON schema of the metadata can be requested by `/v1/versions/{application_version_id}`.
            The schema is located in `input_artifacts.[].metadata_schema`
          examples:
          - checksum_crc32c: 752f9554
            height: 2000
            height_mpp: 0.5
            width: 10000
            width_mpp: 0.5
          title: Metadata
          type: object
        name:
          description: The artifact name according to the application version. List
            of required artifacts is returned by `/v1/versions/{application_version_id}`.
            The artifact names are located in the `input_artifacts.[].name` value
          examples:
          - slide
          title: Name
          type: string
      required:
      - name
      - download_url
      - metadata
      title: InputArtifactCreationRequest
      type: object
    InputArtifactReadResponse:
      properties:
        metadata_schema:
          title: Metadata Schema
          type: object
        mime_type:
          examples:
          - image/tiff
          pattern: ^\w+\/\w+[-+.|\w+]+\w+$
          title: Mime Type
          type: string
        name:
          title: Name
          type: string
      required:
      - name
      - mime_type
      - metadata_schema
      title: InputArtifactReadResponse
      type: object
    ItemCreationRequest:
      properties:
        input_artifacts:
          description: All the input files of the item, required by the application
            version
          items:
            $ref: '#/components/schemas/InputArtifactCreationRequest'
          title: Input Artifacts
          type: array
        reference:
          description: The ID of the slide provided by the caller. The reference should
            be unique across all items of the application run
          examples:
          - case-no-1
          title: Reference
          type: string
      required:
      - reference
      - input_artifacts
      title: ItemCreationRequest
      type: object
    ItemResultReadResponse:
      properties:
        application_run_id:
          description: Application run UUID to which the item belongs
          format: uuid
          title: Application Run Id
          type: string
        error:
          anyOf:
          - type: string
          - type: 'null'
          description: "\nThe error message in case the item is in `error_system`\
            \ or `error_user` state\n    "
          title: Error
        item_id:
          description: Item UUID generated by the Platform
          format: uuid
          title: Item Id
          type: string
        output_artifacts:
          description: "\nThe list of the results generated by the application algorithm.\
            \ The number of files and their\ntypes depend on the particular application\
            \ version, call `/v1/versions/{version_id}` to get\nthe details.\n    "
          items:
            $ref: '#/components/schemas/OutputArtifactResultReadResponse'
          title: Output Artifacts
          type: array
        reference:
          description: The reference of the item from the user payload
          title: Reference
          type: string
        status:
          $ref: '#/components/schemas/ItemStatus'
          description: "\nWhen the item is not processed yet, the status is set to\
            \ `pending`.\n\nWhen the item is successfully finished, status is set\
            \ to `succeeded`, and the processing results\nbecome available for download\
            \ in `output_artifacts` field.\n\nWhen the item processing is failed because\
            \ the provided item is invalid, the status is set to\n`error_user`. When\
            \ the item processing failed because of the error in the model or platform,\n\
            the status is set to `error_system`. When the application_run is canceled,\
            \ the status of all\npending items is set to either `cancelled_user` or\
            \ `cancelled_system`.\n    "
      required:
      - item_id
      - application_run_id
      - reference
      - status
      - error
      - output_artifacts
      title: ItemResultReadResponse
      type: object
    ItemStatus:
      enum:
      - pending
      - canceled_user
      - canceled_system
      - error_user
      - error_system
      - succeeded
      title: ItemStatus
      type: string
    OutputArtifactReadResponse:
      properties:
        metadata_schema:
          title: Metadata Schema
          type: object
        mime_type:
          examples:
          - application/vnd.apache.parquet
          pattern: ^\w+\/\w+[-+.|\w+]+\w+$
          title: Mime Type
          type: string
        name:
          title: Name
          type: string
        scope:
          $ref: '#/components/schemas/OutputArtifactScope'
      required:
      - name
      - mime_type
      - metadata_schema
      - scope
      title: OutputArtifactReadResponse
      type: object
    OutputArtifactResultReadResponse:
      properties:
        download_url:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - type: 'null'
          description: "\nThe download URL to the output file. The URL is valid for\
            \ 1 hour after the endpoint is called.\nA new URL is generated every time\
            \ the endpoint is called.\n    "
          title: Download Url
        metadata:
          description: The metadata of the output artifact, provided by the application
          title: Metadata
          type: object
        mime_type:
          description: The mime type of the output file
          examples:
          - application/vnd.apache.parquet
          pattern: ^\w+\/\w+[-+.|\w+]+\w+$
          title: Mime Type
          type: string
        name:
          description: "\nName of the output from the output schema from the `/v1/versions/{version_id}`\
            \ endpoint.\n    "
          title: Name
          type: string
        output_artifact_id:
          description: The Id of the artifact. Used internally
          format: uuid
          title: Output Artifact Id
          type: string
      required:
      - output_artifact_id
      - name
      - mime_type
      - metadata
      - download_url
      title: OutputArtifactResultReadResponse
      type: object
    OutputArtifactScope:
      enum:
      - item
      - global
      title: OutputArtifactScope
      type: string
    PayloadInputArtifact:
      properties:
        download_url:
          format: uri
          minLength: 1
          title: Download Url
          type: string
        input_artifact_id:
          format: uuid
          title: Input Artifact Id
          type: string
        metadata:
          title: Metadata
          type: object
      required:
      - metadata
      - download_url
      title: PayloadInputArtifact
      type: object
    PayloadItem:
      properties:
        input_artifacts:
          additionalProperties:
            $ref: '#/components/schemas/PayloadInputArtifact'
          title: Input Artifacts
          type: object
        item_id:
          format: uuid
          title: Item Id
          type: string
        output_artifacts:
          additionalProperties:
            $ref: '#/components/schemas/PayloadOutputArtifact'
          title: Output Artifacts
          type: object
      required:
      - item_id
      - input_artifacts
      - output_artifacts
      title: PayloadItem
      type: object
    PayloadOutputArtifact:
      properties:
        data:
          $ref: '#/components/schemas/TransferUrls'
        metadata:
          $ref: '#/components/schemas/TransferUrls'
        output_artifact_id:
          format: uuid
          title: Output Artifact Id
          type: string
      required:
      - output_artifact_id
      - data
      - metadata
      title: PayloadOutputArtifact
      type: object
    RunCreationRequest:
      description: 'Application run payload. It describes which application version
        is chosen, and which user data

        should be processed.'
      properties:
        application_version_id:
          description: Application version ID
          examples:
          - h-e-tme:v1.2.3
          title: Application Version Id
          type: string
        items:
          description: List of the items to process by the application
          items:
            $ref: '#/components/schemas/ItemCreationRequest'
          title: Items
          type: array
      required:
      - application_version_id
      - items
      title: RunCreationRequest
      type: object
    RunCreationResponse:
      properties:
        application_run_id:
          default: Application run id
          format: uuid
          title: Application Run Id
          type: string
      title: RunCreationResponse
      type: object
    RunReadResponse:
      properties:
        application_run_id:
          description: UUID of the application
          format: uuid
          title: Application Run Id
          type: string
        application_version_id:
          description: ID of the application version
          title: Application Version Id
          type: string
        organization_id:
          description: Organization of the owner of the application run
          title: Organization Id
          type: string
        status:
          $ref: '#/components/schemas/ApplicationRunStatus'
          description: "\nWhen the application run request is received by the Platform,\
            \ the `status` of it is set to\n`received`. Then it is transitioned to\
            \ `scheduled`, when it is scheduled for the processing.\nWhen the application\
            \ run is scheduled, it will process the input items and generate the result\n\
            incrementally. As soon as the first result is generated, the state is\
            \ changed to `running`.\nThe results can be downloaded via `/v1/runs/{run_id}/results`\
            \ endpoint.\nWhen all items are processed and all results are generated,\
            \ the application status is set to\n`completed`. If the processing is\
            \ done, but some items fail, the status is set to\n`completed_with_error`.\n\
            \nWhen the application run request is rejected by the Platform before\
            \ scheduling, it is transferred\nto `rejected`. When the application run\
            \ reaches the threshold of number of failed items, the whole\napplication\
            \ run is set to `canceled_system` and the remaining pending items are\
            \ not processed.\nWhen the application run fails, the finished item results\
            \ are available for download.\n\nIf the application run is canceled by\
            \ calling `POST /v1/runs/{run_id}/cancel` endpoint, the\nprocessing of\
            \ the items is stopped, and the application status is set to `cancelled_user`\n\
            \    "
        triggered_at:
          description: Timestamp showing when the application run was triggered
          format: date-time
          title: Triggered At
          type: string
        triggered_by:
          description: Id of the user who triggered the application run
          title: Triggered By
          type: string
        user_payload:
          anyOf:
          - $ref: '#/components/schemas/UserPayload'
          - type: 'null'
          description: Field used internally by the Platform
      required:
      - application_run_id
      - application_version_id
      - organization_id
      - status
      - triggered_at
      - triggered_by
      title: RunReadResponse
      type: object
    TransferUrls:
      properties:
        download_url:
          format: uri
          minLength: 1
          title: Download Url
          type: string
        upload_url:
          format: uri
          minLength: 1
          title: Upload Url
          type: string
      required:
      - upload_url
      - download_url
      title: TransferUrls
      type: object
    UserPayload:
      properties:
        application_id:
          title: Application Id
          type: string
        application_run_id:
          format: uuid
          title: Application Run Id
          type: string
        global_output_artifacts:
          anyOf:
          - additionalProperties:
              $ref: '#/components/schemas/PayloadOutputArtifact'
            type: object
          - type: 'null'
          title: Global Output Artifacts
        items:
          items:
            $ref: '#/components/schemas/PayloadItem'
          title: Items
          type: array
      required:
      - application_id
      - application_run_id
      - global_output_artifacts
      - items
      title: UserPayload
      type: object
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          title: Location
          type: array
        msg:
          title: Message
          type: string
        type:
          title: Error Type
          type: string
      required:
      - loc
      - msg
      - type
      title: ValidationError
      type: object
  securitySchemes:
    OAuth2AuthorizationCodeBearer:
      flows:
        authorizationCode:
          authorizationUrl: https://dev-8ouohmmrbuh2h4vu.eu.auth0.com/authorize
          scopes: {}
          tokenUrl: https://dev-8ouohmmrbuh2h4vu.eu.auth0.com/oauth/token
      type: oauth2
info:
  description: '

    Pagination is done via `page` and `page_size`. Sorting via `sort` query parameter.

    The `sort` query parameter can be provided multiple times. The sorting direction
    can be indicated via

    `+` (ascending) or `-` (descending) (e.g. `/v1/applications?sort=+name)`.'
  title: Aignostics Platform API reference
  version: 1.0.0
openapi: 3.1.0
paths:
  /v1/applications:
    get:
      description: 'Returns the list of the applications, available to the caller.


        The application is available if any of the version of the application is assigned
        to the

        user organization. To switch between organizations, the user should re-login
        and choose the

        needed organization.'
      operationId: list_applications_v1_applications_get
      parameters:
      - in: query
        name: page
        required: false
        schema:
          default: 1
          minimum: 1
          title: Page
          type: integer
      - in: query
        name: page_size
        required: false
        schema:
          default: 50
          maximum: 100
          minimum: 5
          title: Page Size
          type: integer
      - in: query
        name: sort
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Sort
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApplicationReadResponse'
                title: Response List Applications V1 Applications Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: List Applications
      tags:
      - Public
  /v1/applications/{application_id}/versions:
    get:
      description: 'Returns the list of the application versions for this application,
        available to the caller.


        The application version is available if it is assigned to the user''s organization.


        The application versions are assigned to the organization by the Aignostics
        admin. To

        assign or unassign a version from your organization, please contact Aignostics
        support team.'
      operationId: list_versions_by_application_id_v1_applications__application_id__versions_get
      parameters:
      - in: path
        name: application_id
        required: true
        schema:
          title: Application Id
          type: string
      - in: query
        name: page
        required: false
        schema:
          default: 1
          minimum: 1
          title: Page
          type: integer
      - in: query
        name: page_size
        required: false
        schema:
          default: 50
          maximum: 100
          minimum: 5
          title: Page Size
          type: integer
      - in: query
        name: version
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      - in: query
        name: include
        required: false
        schema:
          anyOf:
          - maxItems: 1
            minItems: 1
            prefixItems:
            - type: string
            type: array
          - type: 'null'
          title: Include
      - in: query
        name: sort
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Sort
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApplicationVersionReadResponse'
                title: Response List Versions By Application Id V1 Applications  Application
                  Id  Versions Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: List Versions By Application Id
      tags:
      - Public
  /v1/runs:
    get:
      description: 'The endpoint returns the application runs triggered by the caller.
        After the application run

        is created by POST /v1/runs, it becomes available for the current endpoint'
      operationId: list_application_runs_v1_runs_get
      parameters:
      - description: Optional application ID filter
        in: query
        name: application_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional application ID filter
          title: Application Id
      - description: Optional application version filter
        in: query
        name: application_version
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional application version filter
          title: Application Version
      - description: Request optional output values. Used internally by the platform
        in: query
        name: include
        required: false
        schema:
          anyOf:
          - maxItems: 1
            minItems: 1
            prefixItems:
            - type: string
            type: array
          - type: 'null'
          description: Request optional output values. Used internally by the platform
          title: Include
      - in: query
        name: page
        required: false
        schema:
          default: 1
          minimum: 1
          title: Page
          type: integer
      - in: query
        name: page_size
        required: false
        schema:
          default: 50
          maximum: 100
          minimum: 5
          title: Page Size
          type: integer
      - in: query
        name: sort
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Sort
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/RunReadResponse'
                title: Response List Application Runs V1 Runs Get
                type: array
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: List Application Runs
      tags:
      - Public
    post:
      description: "The endpoint is used to process the input items by the chosen\
        \ application version. The endpoint\nreturns the `application_run_id`. The\
        \ processing fo the items is done asynchronously.\n\nTo check the status or\
        \ cancel the execution, use the /v1/runs/{application_run_id} endpoint.\n\n\
        ### Payload\n\nThe payload includes `application_version_id` and `items` base\
        \ fields.\n\n`application_version_id` is the id used for `/v1/versions/{application_id}`\
        \ endpoint.\n\n`items` includes the list of the items to process (slides,\
        \ in case of HETA application).\nEvery item has a set of standard fields defined\
        \ by the API, plus the metadata, specific to the\nchosen application.\n\n\
        Example payload structure with the comments:\n```\n{\n    application_version_id:\
        \ \"heta:v1.0.0\",\n    items: [{\n        \"reference\": \"slide_1\"  <--\
        \ Input ID to connect the input and the output artifact\n        \"input_artifacts\"\
        : [{\n            \"name\": \"input_slide\"  <-- Name of the artifact defined\
        \ by the application (For HETA it is\"input_slide\")\n            \"download_url\"\
        : \"https://...\" <-- signed URL to the input file in the S3 or GCS. Should\
        \ be valid for more than 6 days\n            \"metadata\": { <-- The metadata\
        \ fields defined by the application. (The example fields set for a slide files\
        \ are provided)\n                \"checksum_crc32c\": \"abc12==\",\n     \
        \           \"mime_type\": \"image/tiff\",\n                \"height\": 100,\n\
        \                \"weight\": 500,\n                \"mpp\": 0.543\n      \
        \      }\n        }]\n    }]\n}\n```\n\n### Response\n\nThe endpoint returns\
        \ the application run UUID. After that the job is scheduled for the\nexecution\
        \ in the background.\n\nTo check the status of the run call `v1/runs/{application_run_id}`.\n\
        \n### Rejection\n\nApart from the authentication, authorization and malformed\
        \ input error, the request can be\nrejected when the quota limit is exceeded.\
        \ More details on quotas is described in the\ndocumentation"
      operationId: create_application_run_v1_runs_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunCreationRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunCreationResponse'
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: Create Application Run
      tags:
      - Public
  /v1/runs/{application_run_id}:
    get:
      description: 'Returns the details of the application run. The application run
        is available as soon as it is

        created via `POST /runs/` endpoint. To download the items results, call

        `/runs/{application_run_id}/results`.


        The application is only available to the user who triggered it, regardless
        of the role.'
      operationId: get_run_v1_runs__application_run_id__get
      parameters:
      - description: Application run id, returned by `POST /runs/` endpoint
        in: path
        name: application_run_id
        required: true
        schema:
          description: Application run id, returned by `POST /runs/` endpoint
          format: uuid
          title: Application Run Id
          type: string
      - in: query
        name: include
        required: false
        schema:
          anyOf:
          - maxItems: 1
            minItems: 1
            prefixItems:
            - type: string
            type: array
          - type: 'null'
          title: Include
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunReadResponse'
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: Get Run
      tags:
      - Public
  /v1/runs/{application_run_id}/cancel:
    post:
      description: 'The application run can be canceled by the user who created the
        application run.


        The execution can be canceled any time while the application is not in a final
        state. The

        pending items will not be processed and will not add to the cost.


        When the application is canceled, the already completed items stay available
        for download.'
      operationId: cancel_application_run_v1_runs__application_run_id__cancel_post
      parameters:
      - description: Application run id, returned by `POST /runs/` endpoint
        in: path
        name: application_run_id
        required: true
        schema:
          description: Application run id, returned by `POST /runs/` endpoint
          format: uuid
          title: Application Run Id
          type: string
      responses:
        '202':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: Cancel Application Run
      tags:
      - Public
  /v1/runs/{application_run_id}/results:
    delete:
      description: 'Delete the application run results. It can only be called when
        the application is in a final

        state (meaning it''s not in `received` or `pending` states). To delete the
        results of the running

        artifacts, first call `POST /v1/runs/{application_run_id}/cancel` to cancel
        the application run.


        The output results are deleted automatically 30 days after the application
        run is finished.'
      operationId: delete_application_run_results_v1_runs__application_run_id__results_delete
      parameters:
      - description: Application run id, returned by `POST /runs/` endpoint
        in: path
        name: application_run_id
        required: true
        schema:
          description: Application run id, returned by `POST /runs/` endpoint
          format: uuid
          title: Application Run Id
          type: string
      responses:
        '204':
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: Delete Application Run Results
      tags:
      - Public
    get:
      description: Get the list of the results for the run items
      operationId: list_run_results_v1_runs__application_run_id__results_get
      parameters:
      - description: Application run id, returned by `POST /runs/` endpoint
        in: path
        name: application_run_id
        required: true
        schema:
          description: Application run id, returned by `POST /runs/` endpoint
          format: uuid
          title: Application Run Id
          type: string
      - description: Filter for items ids
        in: query
        name: item_id__in
        required: false
        schema:
          anyOf:
          - items:
              format: uuid
              type: string
            type: array
          - type: 'null'
          description: Filter for items ids
          title: Item Id  In
      - description: Filter for items by their reference from the input payload
        in: query
        name: reference__in
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          description: Filter for items by their reference from the input payload
          title: Reference  In
      - description: Filter for items in certain statuses
        in: query
        name: status__in
        required: false
        schema:
          anyOf:
          - items:
              $ref: '#/components/schemas/ItemStatus'
            type: array
          - type: 'null'
          description: Filter for items in certain statuses
          title: Status  In
      - in: query
        name: page
        required: false
        schema:
          default: 1
          minimum: 1
          title: Page
          type: integer
      - in: query
        name: page_size
        required: false
        schema:
          default: 50
          maximum: 100
          minimum: 5
          title: Page Size
          type: integer
      - in: query
        name: sort
        required: false
        schema:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Sort
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ItemResultReadResponse'
                title: Response List Run Results V1 Runs  Application Run Id  Results
                  Get
                type: array
          description: Successful Response
        '404':
          description: Application run not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2AuthorizationCodeBearer: []
      summary: List Run Results
      tags:
      - Public
servers:
- url: /api
